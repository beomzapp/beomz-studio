{
  "name": "beomz-temporal-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter '@beomz-studio/temporal-worker...' build",
    "start": "node dist/worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
