{
  "name": "@beomz-studio/temporal-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "echo 'skipping'",
    "build": "pnpm install --frozen-lockfile && pnpm --filter '@beomz-studio/temporal-worker...' build",
    "start": "node dist/worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
