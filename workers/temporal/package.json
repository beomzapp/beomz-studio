{
  "name": "@beomz-studio/temporal-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "echo 'skipping'",
    "build": "pnpm --dir /app install --frozen-lockfile && pnpm --dir /app --filter '@beomz-studio/temporal-worker...' build",
    "start": "node dist/worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
