[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.4", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter '@beomz-studio/temporal-worker...' build"]

[start]
cmd = "node workers/temporal/dist/worker.js"
